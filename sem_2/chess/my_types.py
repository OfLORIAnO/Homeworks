# ? Импорт пакетов
from numpy import array, ndarray
from typing import Tuple, List, Tuple

# ? Создаём типы
symbol_type = list
board_type = ndarray
solutions_type = list[Tuple[int, int]]
total_solutions_type = list[solutions_type]
init_positions_type = List[Tuple[int, int]]
